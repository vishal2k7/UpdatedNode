#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  --harmony "$basedir/../node-es6/index.js" "$@"
  ret=$?
else 
  node  --harmony "$basedir/../node-es6/index.js" "$@"
  ret=$?
fi
exit $ret
